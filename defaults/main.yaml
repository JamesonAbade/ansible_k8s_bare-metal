kubernetes:
  config: "/home/{{ ansible_user }}/.kube"
  CIDR: "10.244.0.0/16"

calico:
  version: "v3.26.4"

metallb:
  addresses: 10.0.8.99/32 # 10.0.8.99
  version: "v0.13.12"

ingress_nginx:
  version: "v1.9.4"
  provider: "cloud"

cert_manager:
  version: "v1.13.3"

letsencrypt:
  email: jamesonabade@gmail.com

nfs:
  whitelist_ips: "10.0.8.0/24" # Permite acesso aos ip do servidor NFS
